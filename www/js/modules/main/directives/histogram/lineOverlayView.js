//import { select } from 'www/js/modules/utils/d3';

var log;

export default class LineOverlayView {

    constructor($log) {
        log = $log;
        this.model = null;
    }

    draw(model) {
        this.model = model;
        // draw bars

        // draw x-axis

        // draw left y-axis

    }
}
